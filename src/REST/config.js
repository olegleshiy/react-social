import { API_URL, API_VERSION } from '../constants/env';

// Config
export const ROOT_URL = API_URL;
export const MAIN_URL = `${API_URL}${API_VERSION}`;
export const groupId = '';
export const invite = '';
